This file is designed to test the basic functionality of the Nature's RPG database.

To ensure it works on your system, run "pip install -r requirements.txt"

This uses psycopg2 to establish a connection to the database and ensure that the SQL commands are sent and executed successfully.
